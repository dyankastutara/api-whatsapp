const moment = require("moment-timezone");
//DB
const Group = require("../../models/mongodb/group");
const Contact = require("../../models/mongodb/contact");

module.exports = {
  update: {
    unsubscribed: async (obj) => {
      try {
      } catch (e) {}
    },
  },
};
